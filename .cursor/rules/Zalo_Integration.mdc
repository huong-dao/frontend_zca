---
alwaysApply: true
---

## Installation
npm install zca-js

## API: loginQR

### Description
Authenticate (login) a Zalo personal account using QR Code.

This API initializes a login session and generates a QR code that must be scanned using the Zalo mobile app.  
After successful scan, the system returns an authenticated API instance used for all further interactions.

---

### Method (zca-js)
zalo.loginQR(options)

---

### Parameters

- userAgent: string (optional)  
  → Custom user agent for the login session

- qrPath: string (optional)  
  → File path to save QR code image  
  → Default: ./qr.png

---

### Example

```ts
import { Zalo } from "zca-js";

const zalo = new Zalo({
    selfListen: false,
    checkUpdate: true,
    logging: true
});

const api = await zalo.loginQR({
    userAgent: "",
    qrPath: "./qr.png"
});

api.listener.start();
```

## API: getQR

### Description
Retrieve QR code(s) of Zalo user(s).

This API returns the QR code associated with one or multiple Zalo user IDs.  
QR codes can be used for identification, sharing contact, or other Zalo-related interactions.

---

### Method (zca-js)
api.getQR(userId)

---

### Parameters

- userId: string | string[] (optional)  
  → Zalo user ID or list of user IDs  
  → If not provided, may return QR of current account (depending on library behavior)

---

### Return
{
  [userId: string]: string; // QR code (base64 or encoded string)
}

### Example

```ts
const result = await api.getQR("user_id_here");
console.log(result);
```

## API: getOwnId

### Description
Retrieve the Zalo ID of the currently logged-in account.

This API returns the unique identifier (`zalo_id`) of the account associated with the current authenticated session.

---

### Method (zca-js)
api.getOwnId()

---

### Parameters

- None

---

### Return
string

---

### Example

```ts
const zaloId = await api.getOwnId();
console.log(zaloId);
```

## API: getAllGroups

### Description
Retrieve the list of all Zalo groups that the current account has joined.

The response includes a version string and a map from each joined group’s **group id** to that group’s **origin name** (`origin_name`): the name Zalo associates with that group in this payload (useful for syncing with your DB `origin_name` / display).

---

### Method (zca-js)
api.getAllGroups()

---

### Parameters

- None

---

### Return

TypeScript shape (matches typical zca-js usage; alias as needed in your project):

```ts
export type GetAllGroupsResponse = {
  version: string;
  /** Keys: Zalo `groupId`. Values: that group’s `origin_name` string. */
  gridVerMap: {
    [groupId: string]: string;
  };
};
```

- `version`: protocol / list version from Zalo.
- `gridVerMap`: map of **`groupId` → `origin_name`** (value is the origin name string for that group).

---

### Example

```ts
const groups: GetAllGroupsResponse = await api.getAllGroups();
console.log(groups.version, groups.gridVerMap);
```

## API: changeGroupName

### Description
Cập nhật **tên nhóm** trên Zalo (API `/api/group/updateinfo` qua zca-js).

Backend Nest: `ZcaApiHelper.changeGroupName` — dùng trong **`PATCH /zalo-groups/:id/group-name`** (đổi tên trên Zalo rồi cập nhật `ZaloGroup` trong DB). Cần **`group_zalo_id`** (grid id) của master và session master có quyền admin nhóm.

---

### Method (zca-js)
api.changeGroupName(name, groupId)

---

### Parameters

- name: string  
  → Tên nhóm mới

- groupId: string  
  → Zalo group grid id (`group_zalo_id` trên thiết bị master)

---

### Return

```ts
export type ChangeGroupNameResponse = {
  status: number;
};
```

`Promise<ChangeGroupNameResponse>`

---

### Example

```ts
api
  .changeGroupName('Tên Mới', '0000000000000000000')
  .then(console.log)
  .catch(console.error);
```

## API: getUserInfo

### Description
Retrieve detailed information of a Zalo user.

This API returns profile data of a user, including basic information such as name and other metadata.  
It also provides change tracking for profile updates.

---

### Method (zca-js)
api.getUserInfo(userId)

---

### Parameters

- userId: string  
  → Zalo user ID

---

### Return
{
  unchanged_profiles: Record<string, unknown>;
  phonebook_version: number;
  changed_profiles: {
    [userId: string]: ProfileInfo;
  };
}

---

### Example

```ts
const userId = "0000000000000000001";

const result = await api.getUserInfo(userId);
console.log(result);
```

## API: sendFriendRequest

### Description
Send a friend request to a Zalo user.

This API allows sending a friend invitation with a custom message to a specific user by their `userId`.  
It is commonly used after retrieving user information via phone number or group data.

---

### Method (zca-js)
api.sendFriendRequest(msg, userId)

---

### Parameters

- msg: string  
  → Message content sent with the friend request

- userId: string  
  → Zalo user ID

---

### Return
Empty string → request sent successfully
---

### Example

#### Send by userId

```ts
const userId = "000000000000000001";
const msg = "Xin chào, hãy kết bạn với tôi!";

await api.sendFriendRequest(msg, userId);
```

#### Send by phone
```ts
const phoneNumber = "0999999999";

const userInfo = await api.findUser(phoneNumber);
const userId = userInfo.uid;

const msg = "Xin chào, hãy kết bạn với tôi!";

await api.sendFriendRequest(msg, userId);
```

## API: getFriendRequestStatus

### Description
Lấy thông tin trạng thái kết bạn giữa tài khoản Zalo hiện đang đăng nhập và một người dùng Zalo cụ thể.

API này hữu ích khi cần kiểm tra lời mời kết bạn đã được chấp nhận chưa mà không phải gọi danh sách bạn bè toàn cục.

---

### Method (zca-js)
api.getFriendRequestStatus(friendId)

---

### Parameters

- friendId: string  
  → Zalo user ID cần kiểm tra trạng thái kết bạn

---

### Return

```ts
export type GetFriendRequestStatusResponse = {
    addFriendPrivacy: number;
    isSeenFriendReq: boolean;
    is_friend: number;
    is_requested: number;
    is_requesting: number;
};
```

---

### Example

```ts
api.getFriendRequestStatus("000000000000000")
    .then(console.log)
    .catch(console.error);
```

## API: removeFriend

### Description
Hủy kết bạn với một người dùng Zalo (theo `friendId` là Zalo user ID).

---

### Method (zca-js)
api.removeFriend(friendId)

---

### Parameters

- friendId: string  
  → Zalo user ID của người cần hủy kết bạn

---

### Return

```ts
export type RemoveFriendResponse = "";
```

---

### Example

```ts
api.removeFriend("0000000000000000000")
    .then(console.log)
    .catch(console.error);
```

## API: acceptFriendRequest

### Description
Accept a friend request from a Zalo user.

This API allows the current account to accept a pending friend request from a specific user using their `userId`.

---

### Method (zca-js)
api.acceptFriendRequest(userId)

---

### Parameters

- userId: string  
  → Zalo user ID who sent the friend request

---

### Example

```ts
const userId = "0000000000000000001";

await api.acceptFriendRequest(userId);
```
---

### Return
- Empty string → request accepted successfully

## API: addUserToGroup

### Description
Add one or multiple users to a Zalo group.

This API allows adding members into a specific group using their `userId`.  
Supports both single user and multiple users in one request.

---

### Method (zca-js)
api.addUserToGroup(memberId, groupId)

---

### Parameters

- memberId: string | string[]  
  → Zalo user ID or list of user IDs to be added into group

- groupId: string  
  → Target group ID

---

### Example

```ts
const groupId = "0000000000000000000";

// Add single member
const memberId = "0000000000000000001";

await api.addUserToGroup(memberId, groupId);

// Add multiple members
const memberIds = [
    "0000000000000000001",
    "0000000000000000002"
];

await api.addUserToGroup(memberIds, groupId);
```

---

### Return
{
  errorMembers: string[];
  error_data: Record<string, string[]>;
}

- errorMembers: string[]  
  → List of userIds that failed to be added

- error_data: object  
  → Detailed error information per user

---

## API: removeUserFromGroup

### Description
Xóa thành viên khỏi nhóm.

Remove one or more members from a Zalo group by their Zalo user id(s), scoped to a `groupId`.

---

### Method (zca-js)
api.removeUserFromGroup(memberId, groupId)

---

### Parameters

- memberId: string | string[]  
  → Zalo user ID or list of user IDs to remove from the group

- groupId: string  
  → Target group ID

---

### Return
`Promise<RemoveUserFromGroupResponse>`

TypeScript:

```ts
export type RemoveUserFromGroupResponse = {
  errorMembers: string[];
};
```

- `errorMembers`: list of user IDs for which removal failed (empty if all succeeded)

---

### Example

```ts
const groupId = "0000000000000000000";

// Remove one member
const memberId = "0000000000000000001";
api
  .removeUserFromGroup(memberId, groupId)
  .then(console.log)
  .catch(console.error);

// Remove several members
const memberIds = ["0000000000000000001", "0000000000000000002"];
api
  .removeUserFromGroup(memberIds, groupId)
  .then(console.log)
  .catch(console.error);
```

---

## API: sendMessage

### Description
Send a message to a Zalo user or group.

This API supports sending:
- Plain text messages
- Formatted messages (bold, color, size, etc.)
- Mentions
- Quotes (reply message)
- Attachments (files)
- Urgent messages

---

### Method (zca-js)
api.sendMessage(message, threadId[, type])

---

### Parameters

- message: string | MessageContent  
  → Message content (plain text or advanced object)

- threadId: string  
  → ID of user or group

- type: ThreadType (optional)  
  → Thread type (User | Group), default = User

---

### Example

#### Send plain text to group

```ts
import { ThreadType } from "zca-js";

await api.sendMessage("Xin chào", "0000000000000000", ThreadType.Group);
```

---

#### Send formatted & important message

```ts
import { ThreadType, Urgency, TextStyle } from "zca-js";

const text = "TIN QUAN TRỌNG!!!";

await api.sendMessage(
    {
        msg: text,
        urgency: Urgency.Important,
        styles: [
            { start: 0, len: text.length, st: TextStyle.Bold },
            { start: 0, len: text.length, st: TextStyle.Red },
            { start: 0, len: text.length, st: TextStyle.Big }
        ]
    },
    "0000000000000000",
    ThreadType.Group
);
```

---

#### Send message with mention

```ts
import { ThreadType } from "zca-js";

await api.sendMessage(
    {
        msg: "Xin chào @ZCA",
        mentions: [
            {
                pos: 9,
                uid: "0000000000000000",
                len: 4
            }
        ]
    },
    "0000000000000000",
    ThreadType.Group
);
```

---

#### Send message with quote (reply)

```ts
api.listener.on("message", async (message) => {
    if (!message.isSelf) {
        await api.sendMessage(
            {
                msg: "ok",
                quote: message
            },
            message.threadId,
            message.type
        );
    }
});
```

---

#### Send attachment

```ts
import path from "path";

api.listener.on("message", async (message) => {
    const isPlainText = typeof message.data.content === "string";

    if (isPlainText && message.data.content === "file") {
        await api.sendMessage(
            {
                msg: "",
                attachments: [path.resolve("./file.txt")]
            },
            message.threadId,
            message.type
        );
    }
});
```

---

### Return

```ts
{
  message: {
    msgId: number;
  } | null;
  attachment: {
    msgId: number;
  }[];
}
```

- message  
  → Result of text message (may be null)

- attachment  
  → List of attachment results

---

### Usage in ZCA

Used in **Message Module**:

#### Send Message Flow

1. Select Zalo sub account
2. Validate:
   - Is member of group
   - Not spam (time config)
3. Call `sendMessage`
4. Save:
   - messageId
   - content
   - sender
   - group
   - timestamp

#### Backend (this repo)

- **Helper:** `ZcaApiHelper.prototype.sendMessage` in `src/zalo/zca-api.helper.ts` — thin wrapper around `api.sendMessage(message, threadId, type)` from zca-js. The first argument may be a **string** or a **`MessageContent` object** (e.g. `{ msg, attachments: [absolute file paths] }` per the zca-js docs above). Re-export: `ThreadType` from the same module (and `src/zalo/index.ts`).
- **App send (child account + `messages` rows):** `POST /messages/send` with **`multipart/form-data`**: `zaloAccountId`, `groupId`, optional `text`, optional repeated `files`. The service writes uploaded buffers to temp paths, calls `ZcaApiHelper.sendMessage` with `MessageContent` when files are present, then deletes the temp files. **Implementation note:** zca-js finishes many file uploads only after a WebSocket `file_done` event; the backend therefore calls `api.listener.start()`, waits for `cipher_key`, runs `sendMessage`, then `api.listener.stop()` (same requirement as the zca-js README for `listener.start()`). **Nhiều “bubble” trên Zalo:** với nội dung kiểu chữ + nhiều file (ví dụ 2 PDF), thư viện thường gửi **từng phần** (một lần gọi `sendMessage` vẫn trả về `result.message` cho đoạn chữ và `result.attachment[]` — mỗi phần tử tương ứng một file). Trong app Zalo bạn sẽ thấy **nhiều tin** (1 dòng chữ + 1 tin/file). Backend lưu **một bản ghi `messages` cho mỗi bubble** (cùng `sent_at`, `message_zalo_id` / `cli_msg_id` riêng). Bản ghi bubble chữ có `parent_id` null; mỗi bản ghi file trong cùng lần gửi trỏ `parent_id` về id dòng chữ — để frontend biết nhóm tin; **`POST /messages/undo/:id` với id dòng chữ** sẽ gọi `api.undo` lần lượt cho chữ và mọi file con, rồi gắn `RECALL` toàn bộ; thu hồi một dòng file (id con) chỉ thu hồi bubble đó. `message` đầu trong response send trùng bubble chữ, `messages` liệt kê tất cả.
- **Group self-echo for `cliMsgId`:** the **text** bubble’s echo can arrive on the WebSocket *while* `sendMessage` is still running (before file parts finish). The server registers a **pre-send** listener for that `threadId`, buffers matching self echos, then passes them into the post-send merge so the **parent** row gets `cli_msg_id` the same way as file rows (avoids a race where only `merge` after the HTTP return would see file echos, not the text). Matching uses **all** of `msgId` / `messageId` / `globalMsgId` / `realMsgId` on both the HTTP part and the echo so the text part still matches if Zalo uses a different field than our single `parseMsgBlock` “primary” id.
- **HTTP (stored QR session, raw session):** `POST /zalo/actions/send-message` with JSON body:
  - `sessionId` (UUID) — `zalo_login_sessions.id`
  - `text` (string) — required for JSON calls **unless** the server populates `attachmentLocalPaths` internally (e.g. `MessagesService` for uploads). Plain `text`-only is the typical JSON use case.
  - `threadId` (string) — target user or group id for the session
  - `threadType` (optional) — `ThreadType` enum from zca-js; **omit = `ThreadType.Group`** for this endpoint (DM: set `ThreadType.User` explicitly)
  - `attachmentLocalPaths` (optional, internal) — absolute paths; not intended for public JSON clients. For attachments from a browser, use **`POST /messages/send`** with multipart.

---

### `deleteMessage` (zca-js) — not used in this backend

The mobile/web Zalo client exposes **`api.deleteMessage`** for cases such as “delete for me” vs “delete for everyone” on **someone else’s** message. This Nest service **does not** wrap `deleteMessage` (no `ZcaApiHelper.deleteMessage`); the only supported “withdraw your own / app-sent” path here is **`api.undo(UndoPayload, threadId, type)`** (see **API: undo** below) and the HTTP routes **`POST /zalo/actions/undo`** and **`POST /messages/undo/:id`**. Persisting `msgId` / `cliMsgId` after `sendMessage` is described under **Send Message** in this file.

---

## API: getAllFriends

### Description
Retrieve the friend list of the currently logged-in Zalo account.

This API returns a list of all friends associated with the authenticated account.  
Supports pagination via `count` and `page`.

---

### Method (zca-js)
api.getAllFriends()

---

### Parameters

- count: number (optional)  
  → Number of friends per page (default: 20000)

- page: number (optional)  
  → Page number (default: 1)

---

### Example

```ts
const friends = await api.getAllFriends();
console.log(friends);
```

---

### Return

```ts
User[]
```

- Array of User objects  
  → Each item represents a Zalo friend

---

## API: undo
Thu hồi (undo) tin nhắn — **khớp** `UndoPayload` + `threadId` + `type` trong zca-js.

```ts
api.undo(payload, threadId, type?)
```

---

### Parameters

- **payload** `UndoPayload` — `msgId` và `cliMsgId` (cùng kiểu lưu sau `sendMessage` thành công)  
- **threadId** — `grid` (nhóm) hoặc `toid` (DM), giống tham số 2 khi gửi tin  
- **type** (tùy chọn) — `ThreadType` (mặc định zca: `User` nếu bỏ qua; **nhóm app** dùng `ThreadType.Group`)

### Return
Promise<UndoResponse>

```ts
export type UndoPayload = {
    msgId: string | number;
    cliMsgId: string | number;
};

export type UndoResponse = {
    status: number;
};
```

### Examples

```ts
// Thu hồi tin do chính tài khoản gửi (ví dụ sau khi đã có msgId + cliMsgId)
import { ThreadType } from "zca-js";

await api.undo(
    { msgId: message.data.msgId, cliMsgId: message.data.cliMsgId },
    message.threadId,
    message.type,
);
```

#### Backend (this repo)

- **Helper:** `ZcaApiHelper.prototype.undo` in `src/zalo/zca-api.helper.ts` — gọi `api.undo(payload, threadId, type)`.
- **HTTP (session thô):** `POST /zalo/actions/undo` — JSON: `sessionId` (UUID), `msgId`, `cliMsgId`, `threadId` (grid / `toid`), `threadType` (tùy chọn; **bỏ qua = nhóm** giống các action khác).
- **Ứng dụng (JWT, theo bản ghi `messages`):** `POST /messages/undo/:id` — khỏi gửi `sessionId` / `threadId`; server đọc `message_zalo_id`, `cli_msg_id`, map `group_zalo_id`, gọi `undo` rồi gắn `status: RECALL`.

---

## API: findUser

### Description
Find a Zalo user by phone number.

This API retrieves user profile information based on a phone number.  
It is commonly used to convert a phone number into a `userId` (uid) for further operations.

---

### Method (zca-js)
api.findUser(phoneNumber)

---

### Parameters

- phoneNumber: string  
  → Phone number of the user

---

### Example

```ts
const user = await api.findUser("0909090909");
console.log(user);
```

---

### Return

```ts
{
  avatar: string;
  cover: string;
  status: string;
  gender: Gender;
  dob: number;
  sdob: string;
  globalId: string;
  bizPkg: ZBusinessPackage;
  uid: string;
  zalo_name: string;
  display_name: string;
}
```

- uid  
  → Zalo user ID (MOST IMPORTANT)

- display_name / zalo_name  
  → User name

- avatar  
  → Profile image

- Other fields  
  → Additional metadata

---